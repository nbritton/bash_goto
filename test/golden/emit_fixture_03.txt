# ---- compiled by goto.sh: 4 label(s), 5 segment(s) ----
declare -g __GOTO_SHELL=$BASHPID __GOTO_PC=__gt_entry
declare -ga __GOTO_STACK=()
declare -g __GOTO_RC=0
__gt_rc() { return "${1:-0}"; }
declare -F __gt_fault > /dev/null || __gt_fault() {
	printf 'goto: fatal: `goto %s` executed in a subshell or pipeline (pid %s != %s)\n' "$1" "$BASHPID" "$__GOTO_SHELL" >&2
	kill -0 "$__GOTO_SHELL" 2>/dev/null &&
	    kill -s TERM "$__GOTO_SHELL" 2>/dev/null
	exit 70
}
while :; do
  case $__GOTO_PC in
    (__gt_entry)
      __GOTO_PC=dispatch
      (( __GOTO_RC == 0 )) || { [[ $- == *e* ]] || __gt_rc "$__GOTO_RC"; }
    source "$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)/../goto.sh";
    input="aab";
    pos=0;
    state=S0;
      __GOTO_RC=$?
      ;;
    (dispatch)
      __GOTO_PC=state_S0
      (( __GOTO_RC == 0 )) || { [[ $- == *e* ]] || __gt_rc "$__GOTO_RC"; }
    c=${input:pos:1};
    : $(( pos++ ));
    [[ -z $c ]] && { __GOTO_RC=$?; __GOTO_PC=halt; [[ $BASHPID == "$__GOTO_SHELL" ]] || __gt_fault halt; continue 1; };
    { __GOTO_RC=$?; __GOTO_PC="state_$state"; [[ $BASHPID == "$__GOTO_SHELL" ]] || __gt_fault "state_$state"; continue 1; };
      __GOTO_RC=$?
      ;;
    (state_S0)
      __GOTO_PC=state_S1
      (( __GOTO_RC == 0 )) || { [[ $- == *e* ]] || __gt_rc "$__GOTO_RC"; }
    echo "S0 saw '$c'";
    state=S1;
    { __GOTO_RC=$?; __GOTO_PC=dispatch; [[ $BASHPID == "$__GOTO_SHELL" ]] || __gt_fault dispatch; continue 1; };
      __GOTO_RC=$?
      ;;
    (state_S1)
      __GOTO_PC=halt
      (( __GOTO_RC == 0 )) || { [[ $- == *e* ]] || __gt_rc "$__GOTO_RC"; }
    echo "S1 saw '$c'";
    state=S0;
    { __GOTO_RC=$?; __GOTO_PC=dispatch; [[ $BASHPID == "$__GOTO_SHELL" ]] || __gt_fault dispatch; continue 1; };
      __GOTO_RC=$?
      ;;
    (halt)
      __GOTO_PC=__gt_END
      (( __GOTO_RC == 0 )) || { [[ $- == *e* ]] || __gt_rc "$__GOTO_RC"; }
    echo "halted in $state after $(( pos - 1 )) chars"
      __GOTO_RC=$?
      ;;
    (__gt_END) break ;;
    (*) printf "goto: no such label: %s\n" "$__GOTO_PC" >&2; exit 2 ;;
  esac
done
(exit "$__GOTO_RC")
